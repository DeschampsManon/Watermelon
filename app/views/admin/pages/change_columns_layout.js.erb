$("#columns-modal").modal("hide");

var $columns_parent = $iframe.find(".current-container .module-container .row");

$columns_parent.empty();

<% eval(@layout).each do |layout| %>
  $columns_parent.append("<%= j render 'admin/pages/editor/module_container', {column_width: layout } %>");
<% end %>